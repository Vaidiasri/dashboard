{
  "name": "dashboard-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack dashboard with FastAPI backend and React frontend",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && python main.py",
    "dev:frontend": "cd frontend && npm run dev",
    "install:frontend": "cd frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm install --prefix frontend && npm run build:frontend && mkdir -p dist && cp -r frontend/dist/* dist/",
    "preview:frontend": "cd frontend && npx vite preview --host 0.0.0.0 --port ${PORT:-4173}",

    "deploy:check": "echo Backend: backend/ && echo Frontend: frontend/"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "fastapi",
    "react",
    "typescript",
    "postgresql",
    "monorepo"
  ],
  "author": "",
  "license": "MIT"
}
